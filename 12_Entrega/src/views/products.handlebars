{{#if userName}}
    <h1>Bienvenido {{userName}}</h1>
    <a class="btn btn-danger" href="./logout">Cerrar Sesión</a>
{{/if}}

<form class="form-group col">
        <label>Producto</label>
        <input class="input" id="name" type="text" name="name">
        <label>Precio</label>
        <input class="input" id="price" type="number" name="price">
        <label>URL</label>
        <input  class="input" id="thumbnail"  type="text" name="thumbnail">
        <input type="submit" id="send" class="btn btn-success" value="enviar">
</form>

{{#if products}}
<table class="table table-dark">
    <thead>
    <tr>
        <th>Nombre</th>
        <th>Precio</th>
        <th>URL</th>
    </tr>
    </thead>
    <tbody id="prodDetail">
        {{#each products}}
            <tr>
                <td>{{this.name}}</td>
                <td>{{this.price}}</td>
                <td><img src="{{this.thumbnail}}" alt="{{this.name}}"> </td>
            </tr>
        {{/each}}
    </tbody>
</table>
{{/if}}

<a class="btn btn-success" href="/api/chat">Chat</a>